# Purpose: utilize a list of objects of class Restaurant to choose a restaurant based on user input
# Restaurant class has attributes: name, cuisine, price range (integer from 1-5), rating (1-5 stars, float), meal type (coffee, breakfast, lunch)
# Input:
# Output:
